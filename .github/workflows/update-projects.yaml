name: Update Project Gallery

permissions:
  contents: write

on:
  repository_dispatch:
    types: [project-updated]

jobs:

  update-cards:

    runs-on: ubuntu-latest

    steps:
      - name: Receive payload
        id: payload
        run: |
          # testing payload reception
          echo "Payload: ${{ toJson(github.event) }}"